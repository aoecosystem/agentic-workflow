---
description: Read docs/ sources (PDF, docx, md) and populate SCOPE.md + memory/STACK-GUIDANCE.md
---

Run the `import-docs` skill. Read every file under `docs/`, build an app-flow-aware normalized feature map, draft `state/SCOPE.md`, and on user confirmation write `state/SCOPE.md` plus `memory/STACK-GUIDANCE.md`. Emit the Flow coverage report and Gap list before any file write. $ARGUMENTS
