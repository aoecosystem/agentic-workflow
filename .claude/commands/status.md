---
description: Show current foundations pipeline stage, artifacts, and next valid commands.
---

Run the `status` skill. Read `SESSION-STATE/SESSION-STATE.md` and report the
project's current pipeline stage, which deliverables exist, and what
commands are valid next. Read-only — never modify any file.
