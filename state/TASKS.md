# Tasks

<!--
This file is generated by the agent when you run "parse scope".
Do not edit task IDs or the status field manually — agents update these.
You CAN add or adjust acceptance criteria before running "start build".

Status values:
  pending      → not started
  in-progress  → Builder is actively working on it
  in-review    → Builder finished, waiting for QA
  needs-fix    → QA rejected, Builder must fix
  done         → QA approved
  blocked      → dependency not met or human input needed

Assigned agent values:
  orchestrator → Orchestrator manages this (scaffold, setup)
  builder-1..N → Dynamic Builder sub-agent pool (one per independent chain)
  qa           → QA sub-agent

Design fields:
  Design source      → none | Figma | other
  Codegen artifact   → path to generated scaffold/spec in memory/mcp-cache
  Visual baseline refs → frame/node references for UI design-fidelity checks
  Screen MCP URLs    → per-screen Figma URLs with node-id for implementation targets
  Design checklist   → objective UI fidelity checks consumed by QA
  Plugin artifact refs → plugin-derived code/tokens artifact path + compatibility status
  Asset refs         → path to per-screen icon/image/illustration manifest
  Contract refs      → which task owns the backend/web/mobile contract surfaces and current integration status

Product-context addendum fields:
  User value         → short user-facing outcome and important scope boundary
  User flow          → ordered steps describing how the user experiences the feature
  Functional notes   → implementation-shaping behavior, dependencies, data/indexing, pagination, storage, or deferred scope
  Edge cases         → explicit empty/error/short-input/fallback conditions to handle
  Test cases         → numbered functional checks that complement acceptance criteria

Attempt budget fields:
  Attempts           → integer; incremented by Builder each time it enters in-progress
  Max attempts       → hard cap (default: 3); QA must block the task after this many failed reviews
  Attempt log        → bulleted list of brief notes per attempt (what was tried, why it failed)
-->

---

## Build progress

| Total | Done | In review | In progress | Needs fix | Blocked | Pending |
|-------|------|-----------|-------------|-----------|---------|---------|
| 0 | 0 | 0 | 0 | 0 | 0 | 0 |
<!-- Agent updates the counts above after each status change.
     Preferred helper: `python3 scripts/update-task-counts.py` -->

---

<!--
TASK TEMPLATE — copy this block for each task generated from SCOPE.md

## TASK-000
- Feature group: Scaffold
- Title: Initialize project structure
- Depends on: none
- Assigned agent: orchestrator
- MCP URL: none
- Contract refs:
  - Backend owner: none
  - Web owner: none
  - Mobile owner: none
  - Integration status: not-started
- Design source: none
- Codegen artifact: none
- Visual baseline refs: none
- Screen MCP URLs: none
- Design checklist:
  - [ ] N/A (non-UI task)
- Plugin artifact refs: none
- Asset refs: none
- User value: N/A (non-user-facing scaffold task)
- User flow:
  - N/A
- Functional notes:
  - N/A
- Edge cases:
  - N/A
- Test cases:
  - N/A
- Files to create/modify:
  - package.json
  - tsconfig.json
  - src/index.ts
- Acceptance criteria:
  - [ ] Project installs and runs with no errors
  - [ ] TypeScript compiles with zero errors
  - [ ] Linting passes on all generated files
- QA notes: (filled by QA agent on rejection)
- Attempts: 0
- Max attempts: 3
- Attempt log:
- Status: pending
-->

<!-- Tasks will be generated here when you run "parse scope" -->
